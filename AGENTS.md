# AGENTS.md

## Wasp

This project uses the Wasp framework (v0.25, see `main.wasp.ts`).

### Docs

- Find docs via the index at https://wasp.sh/llms.txt
- Pick the docs map matching the Wasp version in `main.wasp.ts` (`llms-0.25.txt`).
- Prefer raw Markdown URLs from the docs map over guessing `wasp.sh/docs` URLs.
- If this file conflicts with versioned Wasp docs, trust the docs and tell the user this file may be stale.

### Wasp TypeScript Spec

- `main.wasp.ts` contains the Wasp version and the Wasp app specification.
- When it references app components/functions, import them from `src/` with `with { type: "ref" }`.
- `route(name, ...)` and `crud(name, ...)` take an explicit name.
- Other constructors take no name argument; the declaration's name is the imported identifier. For example, `job(sendReminder, { ... })` declares a job named `sendReminder`.
- `@wasp.sh/spec` is generated by Wasp, don't install it from npm.
- `entities: [...]` in an operation spec only accepts user-defined entities. Do NOT list Wasp-internal entities (e.g. `AuthIdentity`, `Auth`, `Session`) — the compiler rejects them with "Invalid `Entity` reference". For auth-identity lookups use the global `prisma` client from `wasp/server` instead of `context.entities`.
- Prisma enums (`UserRole`) are NOT re-exported from `wasp/entities`. Use string literal types (`"ADMIN" | "EDITOR" | "VIEWER"`).

### Node version

- Wasp 0.25 requires Node >= 24.14.1. The machine's default shell Node is v22 (`/home/nathan/.hermes/node/bin`), which fails with "Your Node.js version (22.22.3) is not supported".
- A Node 24 install lives at `/home/nathan/node24/bin`. Always run Wasp commands with it on PATH:
  ```sh
  export PATH=/home/nathan/node24/bin:$PATH && wasp <command>
  ```
  (`~/.bashrc` / `~/.profile` have been updated to prefer node24, but fresh agents/shells should set it explicitly.)

## Development

- No npm scripts exist (`package.json` has no `scripts` field). Start the app with `wasp start` (background/long-running).
- Client: Vite dev server on `http://100.123.26.95:5173` (bound to the Tailscale IP, `strictPort: true`, see `vite.config.ts`). Server API on port 4001.
- Env: `DATABASE_URL`, `PORT`, and Wasp client/server URLs live in `.env.server`; client vars in `.env.client`.
- DB: PostgreSQL via `DATABASE_URL` in `.env.server` (local Postgres on 55432, db `formbuilder`).

### Verification

- Run `wasp compile` to check if the app is valid (it type-checks server + client + SDK).
- Do not run `tsc` directly for validation.
- After editing `schema.prisma`, run `wasp db migrate-dev --name <name>` — the `--name` flag is REQUIRED (positional args are rejected).

## Architecture notes

- `main.wasp.ts` registers every route/query/action; new pages, queries, and actions must be added there to be reachable from the client.
- All role/access enforcement is hand-written in server code, not Wasp config:
  - Roles: `User.role` is `ADMIN | EDITOR | VIEWER` (default `EDITOR`). VIEWERs cannot create/edit forms.
  - Per-form sharing: `FormAccess.roleId` points at a role in `Form.settings.roles` (defaults: Viewer / Editor / Manager). Capabilities gate submission view/edit/delete (optional `Condition` per capability). Form structure edits require owner or admin. Owner/admin bypass capability conditions.
  - Shared access helpers in `src/server/access.ts` (`getFormAccessForUser`, `resolveRecordPermissions`, `assertCanView/Edit/DeleteSubmission`, `assertIsOwnerOrAdmin/IsAdmin`). Use them in queries/actions.
- Key auth gotcha: operations declared `auth: false` do NOT run Wasp's auth middleware, so `context.user` is always `null` for them even when the requester is logged in. This is why `submitForm` (public, `auth: false`) accepts a `submitterEmail` arg that the client sends from `useAuth()` and the server validates against `AuthIdentity`. Do not read `context.user` in `auth: false` operations and expect an identity.
- System fields (`created_date`, `modified_date`, `updated_by_user`) are stored in the form's `fields` JSON with `readonly: true`. `updated_by_user` has a `valueSource` field (`"email"` default, or `"name"`), resolved server-side in `buildSystemValues` (src/actions.ts). `created_date` is preserved on update; `modified_date`/`updated_by_user` are recomputed.
- `getForms` returns forms with an `access` field (`"owner" | "edit" | "view"`); it EXCLUDES template rows (`Form.isTemplate`). `getFormSubmissions` returns `{ access, roleId, submissions }` where each submission includes `permissions: { view, edit, delete }` for UI gating.
- Templates & import/export: `Form.isTemplate` marks reusable templates. Ops: `getFormTemplates`, `exportForm` (queries), `saveFormAsTemplate`, `duplicateForm`, `createFormFromTemplate`, `importForm` (actions). Templates/duplicates/imports copy only title/fields/settings — never submissions or FormAccess rows. The `/forms/new` builder page has a template picker that seeds the builder client-side (the form row is still created on save).
- Admin GUI: `/admin/users` (`AdminUsersPage`, admin-only; `updateUser` blocks self-demotion). Sharing GUI: `/forms/:id/access` (`FormAccessPage`).
- Smart tags (`{field.KEY}`, `{all_fields}`, `{record_url}`, `{receipt}`, …) are rendered by `renderSmartTags` in `src/shared/smartTags.ts` (client + server safe). Used in email action subject/body templates (server, `formActions.ts`) and in the success message / custom redirect URL (client, `src/pages/FormPage.tsx`).
- Calc/`math` fields support `calcMode`: `"formula"` (default, bracket syntax), `"script"` (client-side JS returning a value via the read-only `CalcFormApi` in `src/shared/calcScript.ts`), or `"query"` (server-side-only async JS that also gets a read-only `db` API from `src/server/calcDb.ts` — `count/sum/avg/min/max/find` over Submission JSON data, `db.join(spec)` for cross-form aggregates via fully parameterized `$queryRawUnsafe` (fixed skeleton; form ids, field keys, values all bound params; field keys also regex-validated), and `{ key: { in: [...] } }` filters (OR-of-equals, capped at 100). All db access is scoped to forms owned by the SAME owner as the calc's form — both sides of a join are checked; cross-owner ids throw; `find` capped at 50 rows, single-form aggregates scan ≤1000, joins have no cap (the 5s race guards them)). Values are submittable, recomputed server-side on every save (`buildCalcValues` in `src/server/calc.ts`, sandboxed via `node:vm` — 100ms sync timeout; query mode is awaited with a 5s external race) and persisted — the stored value is authoritative, never client-sent. The public `recalcCalcField` query (auth: false, per-IP rate-limited, runs ONLY the field's stored script — never client-supplied code) powers live recalc of query-mode fields in the browser; clients show the stored value for query mode otherwise. Calc fields cannot reference other calc fields. `calcMode`/`calcScript` live in the fields JSON, so templates/duplicates/imports copy them automatically.

## Generated code

- `.wasp/` (incl. `.wasp/out/`, `.wasp/spec/`) is generated — never edit it directly.
